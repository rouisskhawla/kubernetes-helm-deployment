apiVersion: v1
kind: Service
metadata:
  name: books-service
  labels:
    app: books-service
spec:
  type: ClusterIP
  selector:
    app: books-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
