name: authors-service
namespace: dev

replicaCount: 1

image:
  repository: khawlarouiss/authors-service
  tag: ""  # Jenkins VERSION

service:
  port: 80
  targetPort: 8082

config:
  LOG_LEVEL: INFO

ingress:
  enabled: false