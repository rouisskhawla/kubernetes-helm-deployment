apiVersion: v1
kind: Service
metadata:
  name: bookstore-frontend
  labels:
    app: bookstore-frontend
spec:
  type: ClusterIP
  selector:
    app: bookstore-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
